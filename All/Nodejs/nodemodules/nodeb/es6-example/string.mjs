/* DEFAULT */
/*
const add = (name) => {
    return `Hello ${name}`;
    //console.log(`Hello ${name}`);
}

export default add;
*/

export const add = (name) => {
    return `Hello ${name}`;
}

export const pow = (name1) => {
    return Math.pow(name1, 2);
}